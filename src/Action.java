public enum Action {
	/*help, start, load, save, quit,
	inventory, i, invt, use,
	move, walk, go, 
	left, forward, right, back,
	attack, defend, run;
	reload
	*/
	move(),
	open_inventory(),
	attack(),
	defend(),
	use(),
	reload_stungun(),
	reload_pistol();
	
}
